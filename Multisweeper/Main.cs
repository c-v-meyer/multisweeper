public class Main{
    private Board board;
    private byte[][];



    public Main(byte size){
        board = new Board(size);
        revealed = new Board(size);


    }

    public boolean isMoveValid(enum PlayerActionType, byte x, byte y){
        if ()
        if(PlayerActionType == Reveal){

            if()

        }

    }
}